#include "main.h"

/**
 * add - adds two integers and returns the result
 *@a: int to be added to c
 *@c: int to be added to a
 * Return: sum of a and c
 */

int add(int c, int a)
{
	int n;

	n = c + a;

	return (n);
}
